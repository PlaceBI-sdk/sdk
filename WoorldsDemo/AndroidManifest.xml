<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.woorldsdemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- to force the app to run at startup -->
        <receiver android:name="com.woorlds.woorldssdk.StartupReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name="com.woorlds.woorldssdk.WoorldsRemoteService" >
            <intent-filter>

                <!-- this intent name must be unique to your app -->
                <action android:name="com.example.woorldsdemo.serviceintent" />
            </intent-filter>
        </service>

        <!-- the following meta-data info must be specified -->
        <meta-data
            android:name="com.woorlds.version"
            android:value="1" />
        
        <!--  repeat the unique intent you have specified in the service -->
        <meta-data
            android:name="com.woorlds.serviceintent"
            android:value="com.example.woorldsdemo.serviceintent" />
        
        <!-- the key that is give to you by woorlds -->
        <meta-data
            android:name="com.woorlds.ApiKey"
            android:value="30bd1805-7deb-4158-b8bb-1b3796f51382" />
        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="7bf629dbba391e4e5fd307b559b5343f7c4a02a7" />
    </application>

</manifest>